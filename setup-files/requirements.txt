# =====================================================================
# RECOMMENDED: do not install this file by hand - run  `python setup_env.py`
# It detects the OS + GPU + driver and installs/REPAIRS the right torch
# build automatically (pip alone will NOT replace an already-installed
# CPU-only torch - it considers it "satisfied").
# =====================================================================

# NOTE: no torch wheel index is pinned here ON PURPOSE - PyTorch retires
# its cuXXX indexes over time, so any hardcoded one WILL eventually break
# (e.g. cu121 has no wheels for new Python/torch versions). setup_env.py
# DISCOVERS the live indexes and picks the newest one your driver supports.
# Plain `pip install torch` is CUDA-enabled on Linux, CPU-only on Windows.

# Core deep-learning stack
torch>=2.2
transformers>=4.44
datasets>=2.20
accelerate>=0.30

# Tokenizers for T5 paraphraser + DeBERTa-v3 verifier
sentencepiece>=0.1.99
protobuf>=3.20

# Numerics & data
numpy>=1.26
pandas>=2.0
pyyaml>=6.0

# Hyper-parameter tuning
optuna>=3.6

# Plots & progress
matplotlib>=3.8
tqdm>=4.66
