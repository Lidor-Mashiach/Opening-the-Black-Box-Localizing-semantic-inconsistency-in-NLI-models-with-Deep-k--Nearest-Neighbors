# Written by tuning/run_tuning.py (Optuna) - best so far after 40 trial(s),
# validation accuracy 0.5272. Read automatically by load_config().
# Updated live on every improvement, so an interrupted study always leaves the best here.
training:
  learning_rate: 8.690336386137876e-06
  batch_size: 16
  epochs: 3
  weight_decay: 0.09923494287492991
  warmup_ratio: 0.15352519954015828
